import { createTheme } from "@mui/material";

export const frameTheme = createTheme({
  palette: {
    primary: {
      main: "#FF7A00",
    },
  },
  typography: {
    h4: {
      fontFamily: "Poppins",
      fontWeight: 300,
    },
  },
});
