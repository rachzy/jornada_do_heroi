import { createTheme } from "@mui/material";

export const fighterTheme = createTheme({
  palette: {
    primary: {
      main: "#DB0000",
    },
  },
  typography: {
    allVariants: {
      fontFamily: "Poppins",
    },
  },
});
